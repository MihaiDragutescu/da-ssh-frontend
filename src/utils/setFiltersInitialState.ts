import { FiltersListType } from '@Types/filtersList';

export const setFiltersInitialState = () => {
  // const queryString = window.location.search;
  // const urlParams = new URLSearchParams(queryString);
  // const filters = ['size', 'brand', 'color', 'collection', 'category'];
  // let initialState: FiltersListType = {};
  // filters.forEach((filter) => {
  //   initialState = {
  //     ...initialState,
  //     [filter]: urlParams.getAll(filter),
  //   };
  // });
  // initialState = {
  //   ...initialState,
  //   minPrice: parseInt(urlParams.get('price_gte') ?? '100'),
  //   maxPrice: parseInt(urlParams.get('price_lte') ?? '9900'),
  // };
  // const [size, setSize] = useQueryParam('size', NumberParam);
  // return initialState;
};
