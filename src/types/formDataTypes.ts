export type newslettersFormDataType = {
  email: string;
};

export type addressFormDataType = {
  firstName: string;
  lastName: string;
  streetAddress: string;
  apartment?: string;
  city: string;
  state: string;
  zipCode: string;
  phone: string;
};
